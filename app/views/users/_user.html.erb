<div id="<%= dom_id user %>">
  <div>
    <strong>Name:</strong>
    <%= user.name %>
  </div>

  <div>
    <strong>Email:</strong>
    <%= user.email %>
  </div>

  <div>
    <strong>Canvas iCal URL:</strong>
    <%= user.ical_url.present? ? "Saved" : "Not added" %>
  </div>

</div>
